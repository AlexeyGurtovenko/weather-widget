import { ILocation } from "../models/location.model";

export const DEDAULT_DEBOUNCE_TIME = 600;

export const DEFAULT_LOCATION = {
    name: "London",
    region: "City of London, Greater London",
    country: "United Kingdom",
    lat: 51.52,
    lon: -0.11,
} as ILocation;
